// This is just to give the address of the .env file which will be used in the database
import dotenv from 'dotenv'
dotenv.config({ path: '../.env' }) //after this line it will allow to use process.env.VARIABLE_NAME from .env file we created


